name: Format code

on: push
#on: pull_request

jobs:
    format:
        #if: github.event.pull_request.merged = 'true'
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2
              with:
                  fetch-depth: 0
            - uses: mibmo/uncrustify-check@v0.1.0
